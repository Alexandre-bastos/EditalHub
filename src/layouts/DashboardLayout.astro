---
import '../styles/global.css';

interface Props {
	title: string;
}

const { title } = Astro.props;

// Obtém o usuário logado do middleware Astro.locals
const user = Astro.locals.user;
const userName = user?.nome || 'Usuário';
const userEmail = user?.email || 'admin@editalhub.com';

// Calcula as iniciais do usuário
const userInitials = userName
  .split(' ')
  .filter(Boolean)
  .map(n => n[0])
  .slice(0, 2)
  .join('')
  .toUpperCase() || 'U';
---

<!doctype html>
<html lang="pt-br" class="dark">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content="EditalHub - Gestão Inteligente de Editais" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title} | EditalHub</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Outfit:wght@400;600;700&display=swap" rel="stylesheet">
	</head>
	<body class="bg-[#0a0a0b] text-gray-100 font-inter min-h-screen flex">
    
    <!-- Sidebar -->
    <aside class="w-64 border-r border-white/5 bg-[#0f0f11] flex flex-col fixed h-full">
      <div class="p-6">
        <h1 class="text-2xl font-outfit font-bold bg-gradient-to-r from-blue-400 to-indigo-500 bg-clip-text text-transparent">
          EditalHub
        </h1>
        <p class="text-[10px] text-gray-500 uppercase tracking-widest mt-1">Inteligência em Concursos</p>
      </div>

      <nav class="flex-1 px-4 space-y-2">
        <a href="/" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/5 transition-all text-gray-400 hover:text-white group">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-hover:text-blue-400"><rect width="7" height="9" x="3" y="3" rx="1"/><rect width="7" height="5" x="14" y="3" rx="1"/><rect width="7" height="9" x="14" y="12" rx="1"/><rect width="7" height="5" x="3" y="16" rx="1"/></svg>
          <span class="font-medium">Dashboard</span>
        </a>
        <a href="/organizadoras" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/5 transition-all text-gray-400 hover:text-white group">
          <!-- Ícone de Prédio/Banca mais adequado -->
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-hover:text-blue-400"><rect x="4" y="2" width="16" height="20" rx="2" ry="2"/><line x1="9" y1="22" x2="9" y2="16"/><line x1="15" y1="22" x2="15" y2="16"/><line x1="9" y1="16" x2="15" y2="16"/><path d="M8 6h2"/><path d="M14 6h2"/><path d="M8 10h2"/><path d="M14 10h2"/></svg>
          <span class="font-medium">Bancas</span>
        </a>
        <a href="/editais" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/5 transition-all text-gray-400 hover:text-white group">
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-hover:text-blue-400"><path d="M14.5 2H6a2 2 0 0 0-2 2v16a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V7.5L14.5 2z"/><polyline points="14.5 2 14.5 7 20 7"/></svg>
          <span class="font-medium">Editais</span>
        </a>
        <a href="/usuarios" class="flex items-center gap-3 px-4 py-3 rounded-xl hover:bg-white/5 transition-all text-gray-400 hover:text-white group">
          <!-- Ícone de Usuários -->
          <svg xmlns="http://www.w3.org/2000/svg" width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" class="group-hover:text-blue-400"><path d="M16 21v-2a4 4 0 0 0-4-4H6a4 4 0 0 0-4 4v2"/><circle cx="9" cy="7" r="4"/><path d="M22 21v-2a4 4 0 0 0-3-3.87"/><path d="M16 3.13a4 4 0 0 1 0 7.75"/></svg>
          <span class="font-medium">Usuários</span>
        </a>
      </nav>

      <!-- Rodapé da Sidebar: Perfil do Usuário com Logout Dinâmico -->
      <div class="p-4 border-t border-white/5">
        <div class="flex items-center justify-between gap-3 px-2 py-2">
          <div class="flex items-center gap-3 min-w-0">
            <div class="w-8 h-8 rounded-full bg-indigo-500 flex items-center justify-center font-bold text-xs shrink-0 select-none text-white shadow-md shadow-indigo-900/30">
              {userInitials}
            </div>
            <div class="min-w-0">
              <p class="text-xs font-semibold text-white truncate" title={userName}>{userName}</p>
              <p class="text-[9px] text-gray-500 font-mono truncate" title={userEmail}>{userEmail}</p>
            </div>
          </div>
          <a 
            href="/logout" 
            class="text-gray-500 hover:text-red-400 hover:bg-red-500/10 p-1.5 rounded-lg transition-all shrink-0 cursor-pointer"
            title="Sair do Painel"
            onclick="return confirm('Deseja realmente sair da sua sessão?');"
          >
            <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
          </a>
        </div>
      </div>
    </aside>

    <!-- Main Content -->
    <main class="flex-1 ml-64 p-8">
      <header class="flex justify-between items-center mb-10">
        <div>
          <h2 class="text-2xl font-outfit font-bold">{title}</h2>
          <p class="text-sm text-gray-500">Bem-vindo ao centro de controle do EditalHub.</p>
        </div>
        <div class="flex gap-4">
          <button id="run-collector" class="bg-white/5 hover:bg-white/10 text-white px-5 py-2.5 rounded-xl font-medium transition-all flex items-center gap-2 border border-white/10 active:scale-95">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M21 12a9 9 0 1 1-6.219-8.56"/><polyline points="22 4 22 10 16 10"/></svg>
            Sincronizar Bancas
          </button>
          <button id="run-processor" class="bg-blue-600 hover:bg-blue-700 text-white px-5 py-2.5 rounded-xl font-medium transition-all flex items-center gap-2 shadow-lg shadow-blue-900/20 active:scale-95">
            <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="m12 3-1.912 5.813a2 2 0 0 1-1.275 1.275L3 12l5.813 1.912a2 2 0 0 1 1.275 1.275L12 21l1.912-5.813a2 2 0 0 1 1.275-1.275L21 12l-5.813-1.912a2 2 0 0 1-1.275-1.275L12 3Z"/><path d="M5 3v4"/><path d="M19 17v4"/><path d="M3 5h4"/><path d="M17 19h4"/></svg>
            Processar com IA
          </button>
        </div>
      </header>

      <slot />
    </main>

    <!-- Floating Sync Status Widget (Premium Glassmorphism Toast) -->
    <div id="sync-status-widget" class="fixed bottom-6 right-6 z-50 bg-[#0d0d11]/85 backdrop-blur-xl border border-blue-500/20 px-5 py-4 rounded-2xl shadow-[0_4px_30px_rgba(0,0,0,0.5),0_0_20px_rgba(59,130,246,0.15)] max-w-sm w-80 transform translate-y-20 opacity-0 pointer-events-none transition-all duration-500 ease-out flex gap-3.5 items-start">
      <div class="flex-shrink-0 mt-0.5" id="sync-widget-icon-container">
        <!-- Spinner Loader Animado -->
        <div class="w-5 h-5 rounded-full border-2 border-blue-500/20 border-t-blue-500 animate-spin"></div>
      </div>
      <div class="flex-1 min-w-0">
        <h4 class="text-xs font-semibold text-white uppercase tracking-wider mb-0.5" id="sync-widget-title">Sincronização Ativa</h4>
        <p class="text-xs text-gray-400 leading-relaxed font-medium" id="sync-widget-message">Verificando status...</p>
        
        <!-- Barra de Progresso Fina -->
        <div class="w-full bg-white/5 h-1 rounded-full mt-2.5 overflow-hidden">
          <div id="sync-widget-progress" class="bg-gradient-to-r from-blue-500 to-indigo-600 h-full rounded-full w-full animate-pulse transition-all duration-1000"></div>
        </div>
      </div>
    </div>

    <!-- Sync Summary Modal (Premium Glassmorphism Overlay) -->
    <div id="sync-summary-modal" class="fixed inset-0 z-50 flex items-center justify-center bg-black/70 backdrop-blur-md opacity-0 pointer-events-none transition-all duration-300">
      <div class="bg-[#0f0f13]/90 border border-white/10 rounded-2xl p-6 max-w-md w-full mx-4 shadow-2xl transform scale-95 transition-all duration-300">
        <div class="flex items-center gap-3 mb-4">
          <div class="p-2 bg-emerald-500/10 rounded-xl text-emerald-400">
            <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 11.08V12a10 10 0 1 1-5.93-9.14"/><polyline points="22 4 12 14.01 9 11.01"/></svg>
          </div>
          <h3 class="text-lg font-outfit font-bold text-white">Sincronização Concluída</h3>
        </div>
        
        <p class="text-sm text-gray-400 mb-6 leading-relaxed">
          O processo de varredura nos portais e processamento com Inteligência Artificial foi finalizado. Confira o resumo do que foi modificado na base de dados:
        </p>
        
        <div class="space-y-3 mb-6 bg-white/[0.02] border border-white/5 rounded-xl p-4 font-inter">
          <div class="flex justify-between items-center text-sm">
            <span class="text-gray-400 flex items-center gap-2">
              <span class="w-2 h-2 rounded-full bg-blue-500"></span>
              Editais Coletados (Novos)
            </span>
            <span class="font-bold font-mono text-gray-200" id="summary-valid">0</span>
          </div>
          <div class="flex justify-between items-center text-sm">
            <span class="text-gray-400 flex items-center gap-2">
              <span class="w-2 h-2 rounded-full bg-emerald-500 animate-pulse"></span>
              Processados com IA (Vagas extraídas)
            </span>
            <span class="font-bold font-mono text-emerald-400" id="summary-processed">0</span>
          </div>
          <div class="flex justify-between items-center text-sm">
            <span class="text-gray-400 flex items-center gap-2">
              <span class="w-2 h-2 rounded-full bg-red-500"></span>
              Erros de Processamento (IA)
            </span>
            <span class="font-bold font-mono text-red-400" id="summary-failed">0</span>
          </div>
          <div class="flex justify-between items-center text-sm border-t border-white/5 pt-2 mt-2">
            <span class="text-gray-500 flex items-center gap-2">
              <span class="w-2 h-2 rounded-full bg-gray-600"></span>
              Arquivos Filtrados / Descartados
            </span>
            <span class="font-bold font-mono text-gray-400" id="summary-ignored">0</span>
          </div>
        </div>
        
        <button id="close-summary-btn" class="w-full py-2.5 bg-blue-600 hover:bg-blue-500 text-white font-bold rounded-xl transition-all shadow-lg shadow-blue-600/20 active:scale-[0.98]">
          Entendido (OK)
        </button>
      </div>
    </div>

    <script>
      let isPollerActive = false;
      let pollerIntervalId: any = null;
      let lastActiveState = false;

      // Elementos do Widget
      const widget = document.getElementById('sync-status-widget');
      const widgetIcon = document.getElementById('sync-widget-icon-container');
      const widgetTitle = document.getElementById('sync-widget-title');
      const widgetMessage = document.getElementById('sync-widget-message');
      const widgetProgress = document.getElementById('sync-widget-progress');

      // Botões no layout
      const runBtn = document.getElementById('run-collector');
      const processBtn = document.getElementById('run-processor');

      function getAllSyncButtons() {
        return document.querySelectorAll('#run-collector, #sync-btn, #run-processor, .process-btn');
      }

      function disableAllButtons(disable: boolean) {
        const buttons = getAllSyncButtons();
        buttons.forEach(btn => {
          if (disable) {
            btn.setAttribute('disabled', 'true');
            btn.classList.add('opacity-40', 'pointer-events-none');
          } else {
            btn.removeAttribute('disabled');
            btn.classList.remove('opacity-40', 'pointer-events-none');
          }
        });

        const syncOrgSelect = document.getElementById('sync-org-select');
        if (syncOrgSelect) {
          if (disable) {
            syncOrgSelect.setAttribute('disabled', 'true');
            syncOrgSelect.classList.add('opacity-40', 'pointer-events-none');
          } else {
            syncOrgSelect.removeAttribute('disabled');
            syncOrgSelect.classList.remove('opacity-40', 'pointer-events-none');
          }
        }
      }

      function showWidget(stage: string, message: string) {
        if (!widget) return;
        
        // Exibe o widget
        widget.classList.remove('translate-y-20', 'opacity-0', 'pointer-events-none');
        widget.classList.add('translate-y-0', 'opacity-100');
        
        // Estilos padrão de atividade (azul)
        widget.classList.remove('border-green-500/30');
        widget.classList.add('border-blue-500/20');

        if (widgetProgress) {
          widgetProgress.classList.remove('bg-emerald-500', 'w-full');
          widgetProgress.classList.add('bg-gradient-to-r', 'from-blue-500', 'to-indigo-600', 'animate-pulse');
          if (stage === 'scraping') {
            widgetProgress.style.width = '30%';
          } else {
            widgetProgress.style.width = '75%';
          }
        }

        if (widgetIcon) {
          widgetIcon.innerHTML = `<div class="w-5 h-5 rounded-full border-2 border-blue-500/20 border-t-blue-500 animate-spin"></div>`;
        }

        if (widgetTitle) {
          widgetTitle.innerText = stage === 'scraping' ? 'Buscando Editais' : 'Processamento IA';
          widgetTitle.classList.remove('text-green-400');
          widgetTitle.classList.add('text-white');
        }

        if (widgetMessage) {
          widgetMessage.innerText = message;
        }

        disableAllButtons(true);
      }

      function showWidgetSuccess(summary: any) {
        if (!widget) return;

        if (widgetIcon) {
          widgetIcon.innerHTML = `
            <svg class="text-green-400 w-5 h-5" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
              <polyline points="20 6 9 17 4 12"/>
            </svg>
          `;
        }

        if (widgetTitle) {
          widgetTitle.innerText = 'Sincronizado!';
          widgetTitle.classList.remove('text-white');
          widgetTitle.classList.add('text-green-400');
        }

        if (widgetMessage) {
          widgetMessage.innerText = 'Processo concluído com sucesso!';
        }

        if (widgetProgress) {
          widgetProgress.classList.remove('from-blue-500', 'to-indigo-600', 'animate-pulse');
          widgetProgress.classList.add('bg-emerald-500');
          widgetProgress.style.width = '100%';
        }

        widget.classList.remove('border-blue-500/20');
        widget.classList.add('border-green-500/30');

        // Preenche os dados no modal
        const modal = document.getElementById('sync-summary-modal');
        const modalContent = modal?.querySelector('div');
        const validEl = document.getElementById('summary-valid');
        const processedEl = document.getElementById('summary-processed');
        const failedEl = document.getElementById('summary-failed');
        const ignoredEl = document.getElementById('summary-ignored');

        if (summary) {
          if (validEl) validEl.innerText = summary.valid ?? '0';
          if (processedEl) processedEl.innerText = summary.processed ?? '0';
          if (failedEl) failedEl.innerText = summary.failed ?? '0';
          if (ignoredEl) ignoredEl.innerText = summary.ignored ?? '0';
        }

        // Abre o modal com uma animação de fade e escala suave
        setTimeout(() => {
          if (modal && modalContent) {
            modal.classList.remove('opacity-0', 'pointer-events-none');
            modalContent.classList.remove('scale-95');
            modalContent.classList.add('scale-100');
          }
        }, 1000);
      }

      function hideWidget() {
        if (!widget) return;
        widget.classList.remove('translate-y-0', 'opacity-100');
        widget.classList.add('translate-y-20', 'opacity-0', 'pointer-events-none');
        disableAllButtons(false);
      }

      async function checkSyncStatus() {
        try {
          const res = await fetch('/api/sync-status');
          if (!res.ok) return;
          
          const data = await res.json();
          
          if (data.active) {
            lastActiveState = true;
            showWidget(data.stage, data.message);
          } else {
            if (lastActiveState) {
              // Mudou de ativo para inativo agora: sucesso!
              lastActiveState = false;
              if (pollerIntervalId) clearInterval(pollerIntervalId);
              showWidgetSuccess(data.summary);
            } else {
              hideWidget();
            }
          }
        } catch (err) {
          console.error('Erro ao verificar status de sync:', err);
        }
      }

      // Inicia Polling em segundo plano
      function startPoller() {
        if (isPollerActive) return;
        isPollerActive = true;
        
        // Checagem imediata
        checkSyncStatus();
        
        // Checagens a cada 6 segundos
        pollerIntervalId = setInterval(checkSyncStatus, 6000);
      }

      // Expõe no objeto global window para uso em scripts de outras páginas
      (window as any).startPoller = startPoller;


      // Disparador de Coleta
      runBtn?.addEventListener('click', async () => {
        runBtn.setAttribute('disabled', 'true');
        const originalHtml = runBtn.innerHTML;
        runBtn.innerHTML = 'Disparando...';
        
        try {
          const res = await fetch('/api/collect', { method: 'POST' });
          if (res.ok) {
            // Inicia imediatamente o poller para dar feedback ao usuário
            startPoller();
          } else {
            alert('Falha ao disparar sincronização na Azure');
            runBtn.removeAttribute('disabled');
            runBtn.innerHTML = originalHtml;
          }
        } catch (err) {
          alert('Erro de rede ao iniciar sincronização');
          runBtn.removeAttribute('disabled');
          runBtn.innerHTML = originalHtml;
        }
      });

      // Disparador de Processamento em Lote
      processBtn?.addEventListener('click', async () => {
        processBtn.setAttribute('disabled', 'true');
        const originalHtml = processBtn.innerHTML;
        processBtn.innerHTML = 'Disparando...';
        
        try {
          const res = await fetch('/api/process', { method: 'POST' });
          if (res.ok) {
            // Inicia imediatamente o poller
            startPoller();
          } else {
            alert('Falha ao iniciar processamento na Azure');
            processBtn.removeAttribute('disabled');
            processBtn.innerHTML = originalHtml;
          }
        } catch (err) {
          alert('Erro de rede ao iniciar processamento');
          processBtn.removeAttribute('disabled');
          processBtn.innerHTML = originalHtml;
        }
      });

      // Ouvinte para o botão "Entendido (OK)" no modal de resumo
      const closeSummaryBtn = document.getElementById('close-summary-btn');
      closeSummaryBtn?.addEventListener('click', () => {
        const modal = document.getElementById('sync-summary-modal');
        const modalContent = modal?.querySelector('div');
        
        if (modal && modalContent) {
          modalContent.classList.remove('scale-100');
          modalContent.classList.add('scale-95');
          modal.classList.add('opacity-0', 'pointer-events-none');
        }
        
        // Recarrega a página para atualizar toda a lista e os novos editais e badges
        window.location.reload();
      });

      // Inicialização: Se houver qualquer sync rodando ao carregar a página,
      // ativa automaticamente o widget flutuante e o poller
      startPoller();
    </script>
	</body>
</html>


<style is:global>
	:root {
		--accent: 136, 58, 234;
		--accent-light: 224, 204, 250;
		--accent-dark: 49, 10, 101;
		--accent-gradient: linear-gradient(
			45deg,
			rgb(var(--accent)),
			rgb(var(--accent-light)) 30%,
			white 60%
		);
	}
  
  .font-outfit { font-family: 'Outfit', sans-serif; }
  .font-inter { font-family: 'Inter', sans-serif; }

  /* Glassmorphism scrollbar */
  ::-webkit-scrollbar { width: 8px; }
  ::-webkit-scrollbar-track { background: transparent; }
  ::-webkit-scrollbar-thumb { background: rgba(255,255,255,0.1); border-radius: 10px; }
  ::-webkit-scrollbar-thumb:hover { background: rgba(255,255,255,0.2); }
</style>
